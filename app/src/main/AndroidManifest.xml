<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="miranda.miranda.thecheezery">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icono"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetalleCombo"></activity>
        <activity android:name=".ComboActivity" />
        <activity android:name=".SaltiesActivity" />
        <activity android:name=".sweets_activity" />
        <activity android:name=".hotdrinks_activity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ProductosActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>